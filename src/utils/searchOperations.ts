
import { 
  enhanceSearchQuery, 
  executeSearch, 
  loadMore, 
  validateSearchParams 
} from './search/operations';

// Re-export all functions from the new structure
export {
  enhanceSearchQuery,
  executeSearch,
  loadMore,
  validateSearchParams
};
